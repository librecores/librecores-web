{% extends "@FOSUser/layout.html.twig" %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
  <div class="mx-auto librecores-user-signupin-panel">
    <div class="card">
      <div class="card-header">
        <h3 class="card-title">Resend confirmation email</h3>
      </div>
      <div class="card-body">
        {{ form_start(form) }}
          {{ form_row(form.user) }}
          <button class="btn btn-success btn-block" type="submit" id="_submit" name="_submit">Resend email</button>
        {{ form_end(form) }}
        <hr/>
        <div class="text-center">
          Don't have an account yet?<br/>
          <a href="{{ path('fos_user_registration_register') }}" style="font-weight: bold">Register now</a>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
