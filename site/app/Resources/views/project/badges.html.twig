{% extends 'layout.html.twig' %}

{% block title %}
{{ project.getFullName() }} @ LibreCores: Badges
{% endblock %}

{% block stylesheets %}
  {{ parent() }}
{% endblock %}

{% block pagepath %}Home &raquo; Projects &raquo; {{ project.getFullName() }} &raquo; Badges{% endblock %}

{% block content %}
<h1 class="projectname"><a href="{{ path('librecores_project_repo_project_view', {'parentName':
  project.getParentName(), 'projectName': project.getName()}) }}">{{ project.getFullName() }}</a>: Badges</h1>

<div class="row">
  <div class="col-sm-2">
    <ul class="list-group">
      <li class="list-group-item"><a href="{{ path('librecores_project_repo_project_view', {'parentName':
    project.getParentName(), 'projectName': project.getName()}) }}">
        <i class="fa fa-bookmark fa-fw"></i>
        Project
      </a></li>
      <li class="list-group-item"><a href="{{ path('librecores_project_repo_project_settings', {'parentName':
    project.getParentName(), 'projectName': project.getName()}) }}">
        <i class="fa fa-cogs fa-fw"></i>Settings
      </a></li>
      <li class="list-group-item active"><a href="#">
        <i class="fa fa-cogs fa-fw"></i>Badges
      </a></li>
    </ul>
    <br>
  </div>
  <div class="col-sm-10">
    <p>Show others that your project is listed on LibreCores and add a badge, e.g. to the README file in the repository!
    We have prepared a couple badges for you to use for different document formats, ready for copy-and-pasting.</p>

    <div>
      <img src="{{ badgeSvgUrl }}" alt="LibreCores"/>
    </div>

    <div style="margin-top: 10px">
      <strong>Markdown</strong>
      <textarea rows="2" class="form-control" style="font-family: monospace">[![LibreCores]({{ badgeSvgUrl }})]({{ badgeUrl }})</textarea>
    </div>

    <div style="margin-top: 10px">
      <strong>HTML</strong>
      <textarea rows="3" class="form-control" style="font-family: monospace"><a href="{{ badgeUrl }}" title="LibreCores">
  <img src="{{ badgeSvgUrl }}" alt="LibreCores"/>
</a></textarea>
    </div>

    <div style="margin-top: 10px">
      <strong>reStructuredText (RST)</strong>
      <textarea rows="2" class="form-control" style="font-family: monospace">.. image:: {{ badgeSvgUrl }}
   :target: {{ badgeUrl }}</textarea>
    </div>

    <p style="margin-top: 20px">
    If you prefer another style, you can replace <tt>flat</tt> in the links above with <tt>flat-square</tt> or <tt>plastic</tt>.
    </p>

  </div>
</div>
{% endblock %}

{% block javascripts %}
  {{ parent() }}
  {{ encore_entry_script_tags('project_settings') }}
{% endblock %}
