{% extends 'base.html.twig' %}

{% block body %}

{#<div class="librecores-searchbar">
<div class="container">
  <div class="pull-left">something -> something -> asdf{% block pagepath %}{% endblock %}</div>
  <div class="pull-right">
    <form class="form-inline">
      <input type="text" class="form-control"  placeholder="What are you looking for?">
      <button class="btn btn-default text-uppercase" type="submit">
        <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
        Find IP Core
      </button>
    </form>
  </div>
</div>
</div>#}

<div class="container" id="maincontent">
  {# flash messages, i.e. "user account has been created" #}
  {% if app.request.hasPreviousSession %}
    {% for type, messages in app.session.flashbag.all() %}
      {% for message in messages %}
        <div class="alert alert-{{ type }}">
          {{ message }}
        </div>
      {% endfor %}
    {% endfor %}
  {% endif %}

  {# the main content #}
  {% block content %}{% endblock %}
</div>
{% endblock %}
